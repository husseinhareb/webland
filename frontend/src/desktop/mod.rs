//! The Webland shell: panels, dock, launcher, window chrome.
//!
//! Placeholder so the frontend has something to render. Real work starts in
//! Phase 5 (see docs/roadmap.md), once the transport is proven.

use leptos::prelude::*;

#[component]
pub fn Desktop() -> impl IntoView {
    view! { <main>"webland"</main> }
}
